import { AppShell } from "@/components/AppShell"

function App() {
  return (
    <AppShell>
      <p className="text-muted-foreground">
        The rack is quiet. Pages arrive in the tasks that follow.
      </p>
    </AppShell>
  )
}

export default App
